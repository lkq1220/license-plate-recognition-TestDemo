1.在Makefile所在目录下执行make命令，生成CharsTrain程序。

2.在CharsTrain所在目录下执行./charsTrain.sh命令，处理img文件夹中的图片，生成多层感知机模型保存在tmp文件夹下。

3.在Makefile所在目录下执行make clean命令，清除生成的CharsTrain程序和tmp文件夹下的多层感知机模型。

备注：

1.执行./renameImage.sh命令，可以对img文件夹中的图片重命名。
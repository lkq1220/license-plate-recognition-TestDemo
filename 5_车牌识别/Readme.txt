1.在Makefile所在目录下执行make命令，生成PlateRecognition程序。

2.在PlateRecognition所在目录下执行./PlateRecognition <image>命令，处理img文件夹中的图片。

3.在Makefile所在目录下执行make clean命令，清除生成的PlateRecognition程序。

备注：

1.执行./renameImage.sh命令，可以对img文件夹中的图片重命名。
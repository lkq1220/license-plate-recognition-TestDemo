1.在Makefile所在目录下执行make命令，生成CharsSegment程序。

2.在CharsSegment所在目录下执行./charsSegment.sh命令，处理img文件夹中的图片，生成图片保存在tmp文件夹下。

3.在Makefile所在目录下执行make clean命令，清除生成的CharsSegment程序和tmp文件夹下的图片。

备注：

1.执行./renameImage.sh命令，可以对img文件夹中的图片重命名。